fn ttt() {
  // for loop with an index
  const looper = [1,2,3]
  for (i,val) in looper.iter().enumerate() {println!("looper i=¦{i}¦ vl=¦{val}¦")};
}

fn tt() {
  key2bit.iter().find(|&&f) f == key_seq_user_def)
  let desired = "control";
  let to_eat = Ctrl.iter().find(|&&f| f == desired); //Some("control")
  p!("{:?}", to_eat);
  let desired = "ctrl1";
  let to_eat = Ctrl.iter().find(|&&f| f == desired); //None
  p!("{:?}", to_eat);
  p!("¦{}¦", "⎈".to_lowercase()); //⎈

}